export function getIncomingInnerHtml () {
  const html = `
  <header class="incoming-header">
    Incoming
  </header>
  <main class="incoming-main"></main>
  <fiooter class="incoming-footer"></fiooter>
  `

  return html;
}
